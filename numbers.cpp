#include <iostream>

using namespace std;
int main()
{
    int x=10;
    // we have int,long,long long 
    // to store the bigger integers

    // depending upon the siz of the number we can choose the datatype

    long y=100000000;
    long long z=100000000000000000;

    cout<<x<<" "<<y<<" "<<z<<endl;

    // for the decimal numbers we use the float and double

    float f=10.3;
    double d=10.45324;
    cout<<f<<" "<<d;
    
}
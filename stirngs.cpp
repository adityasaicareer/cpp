#include <iostream>

using namespace std;

int main()
{
    string s1;
    // here we use the cin it wil only takes the input unpto the space
    // we have to use the getline to take the complete line

    // string s2;
    // cin>>s1;
    // cout<<s1;
    string s2;
    getline(cin,s2);
    cout<<s2<<endl;

    

}